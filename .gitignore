.env
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
local-data/
whatsapp_bot.db
*.db
credentials.json
rag_traces/

# Lead data / customer PII — never commit
*.xlsx

# Large source archives (KB_extracted is the unpacked version, already tracked)
*.zip

# External reference clones (have their own .git)
reference_repos/

# Runtime data, logs, exports, screenshots
var/
export_log/
log.txt
images/
*.png

# Superseded asset from the removed flyer feature (gitignore matches spaces literally, no quotes)
Timmins Training Consulting Sdn Bhd - Training Calendar - July August 2026.pdf

# RAG source content — now served from Railway Postgres (index lives in rag_chunks).
# Kept in local-data/rag-sources/ for rebuilds; out of the repo per the no-data-in-git goal.
NEW_Courses/
KB_extracted/
TIMMINS TRAINING - Meta Lead sautomation.docx

# Editor settings are personal, not project config
.vscode/
.idea/

# Content exported from the database for backup — regenerate with scripts/export_content_from_db.py
content_export/
